# Test compose — foundation scaffold only (no external services)
services:
  platform-test:
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      ENVIRONMENT: development
      APP_NAME: trading-platform
      SCHEMA_VERSION: "1.0.0"
      TIMEZONE_INTERNAL: UTC
    command: ["python", "scripts/validate_phase0.py"]
